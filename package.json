{
  "name": "trade-ai",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Trade AI — XAUUSD & Forex (bitta terminal: npm install → npm run dev)",
  "scripts": {
    "setup": "node scripts/setup.js",
    "postinstall": "node scripts/setup.js",
    "dev": "concurrently -k -n API,WEB -c blue,green \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "node scripts/start-backend.js",
    "dev:web": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
